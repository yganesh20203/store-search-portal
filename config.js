// config.js
const CONFIG = {
    // 1. From Google Cloud Console > Credentials
    CLIENT_ID: "YOUR_ACTUAL_CLIENT_ID_HERE.apps.googleusercontent.com", 
    
    // 2. The API Key is optional for this specific flow, but good to have if needed later
    // API_KEY: "YOUR_API_KEY_HERE", 

    // 3. The exact folder ID where your data lives (we will need this later)
    ROOT_FOLDER_ID: "https://drive.google.com/drive/folders/1jguubICcAtMijiaNETV7B35G2YZlyM5O" 
};
